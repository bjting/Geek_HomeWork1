# 查询结果导出功能设计思路

## 1. 功能概述

为数据库查询工具新增数据导出功能，允许用户将SQL查询结果导出为多种常见格式，便于数据分析、报告生成和与其他系统的数据交换。

### 核心功能
- 支持三种导出格式：CSV、JSON、Excel
- 前端提供格式选择器，用户交互友好
- 后端统一处理导出逻辑，确保数据格式标准化
- 支持大结果集的分页导出（默认限制1000行）
- 提供文件下载功能，自动处理文件名和MIME类型

## 2. 设计思路

### 2.1 架构设计

采用前后端分离的架构设计：

**前端层**
- **UI组件**: `ExportFormatSelector` - 格式选择器模态框
- **服务层**: `exportService` - 封装导出API调用
- **集成层**: 在查询执行页面集成导出按钮和交互逻辑

**后端层**
- **API层**: `/api/v1/dbs/{name}/export` - 统一导出端点
- **服务层**: `ExportService` - 导出业务逻辑封装
- **数据层**: 查询执行 + 格式转换 + 文件生成

### 2.2 数据流程设计

```
用户点击导出按钮
    ↓
显示格式选择器（CSV/JSON/Excel）
    ↓
用户选择格式并确认
    ↓
前端发送POST请求到导出API
    ↓
后端执行SQL查询获取结果
    ↓
根据选择的格式调用对应的导出方法
    ↓
生成文件内容（内存中处理）
    ↓
通过StreamingResponse返回文件流
    ↓
前端触发浏览器下载
```

## 3. 技术实现细节

### 3.1 格式支持策略

#### CSV格式
- 使用Python内置的`csv`模块
- 包含列头和所有行数据
- 支持中文字符和特殊字符
- 兼容Excel、Google Sheets等工具

#### JSON格式
- 结构化数据，包含元信息
- 数据结构：
  ```json
  {
    "columns": ["column1", "column2"],
    "rows": [{"column1": "value1", "column2": "value2"}],
    "total_rows": 100,
    "exported_rows": 100
  }
  ```
- 适合程序化处理和API集成

#### Excel格式
- 使用`pandas`和`openpyxl`库
- 生成真正的`.xlsx`文件
- 保留数据类型和格式
- 支持多工作表（当前单工作表）

### 3.2 性能优化策略

#### 内存管理
- 使用内存缓冲区（`StringIO`、`BytesIO`）而非临时文件
- 避免大文件对磁盘的I/O压力
- 支持流式响应，减少服务器内存占用

#### 数据限制
- 默认最大导出1000行，避免大查询性能问题
- 可配置的行数限制（1-1000行）
- 提供实际导出行数的反馈

#### 查询复用
- 导出时重新执行SQL查询，确保数据一致性
- 复用现有的查询执行服务
- 统一的SQL验证和错误处理

### 3.3 用户体验设计

#### 交互流程
1. 查询执行完成后显示"导出"按钮
2. 点击按钮弹出格式选择器
3. 每种格式提供简短说明
4. 显示最大行数限制提示
5. 导出过程中显示加载状态
6. 成功/失败的消息反馈

#### 文件处理
- 自动生成文件名：`query_results.csv/json/xlsx`
- 从响应头解析文件名（支持自定义）
- 使用Blob API触发浏览器下载
- 自动清理临时对象URL

#### 错误处理
- 查询执行失败的友好提示
- 导出格式错误的验证
- 网络错误的重试机制
- 详细的错误日志记录

## 4. 技术选型

### 后端技术
- **导出引擎**:
  - CSV: `csv` (Python标准库)
  - JSON: `json` (Python标准库)
  - Excel: `pandas` + `openpyxl`
- **响应处理**: `FastAPI.StreamingResponse`
- **类型验证**: `pydantic`模型

### 前端技术
- **UI组件**: Ant Design (Modal, Radio, Button)
- **HTTP客户端**: Axios
- **文件处理**: Browser Blob API
- **状态管理**: React Hooks

## 5. 扩展性考虑

### 格式扩展
- 服务层采用策略模式，易于添加新格式
- 统一的导出接口：`export(format, columns, rows, max_rows)`
- 新增格式只需实现对应的导出方法

### 功能扩展
- 支持自定义文件名
- 支持导出模板
- 支持批量导出
- 支持导出历史记录
- 支持导出进度显示

### 性能扩展
- 支持异步大数据集导出
- 支持分批次导出
- 支持后台任务处理
- 支持导出文件缓存

## 6. 安全性考虑

### 权限控制
- 继承数据库连接的权限验证
- 防止敏感数据泄露
- SQL注入防护（复用现有验证）

### 资源保护
- 限制单次导出的数据量
- 防止恶意大查询
- 限制并发导出请求数

### 数据安全
- 导出内容与原始查询结果一致
- 避免数据格式转换导致的信息丢失
- 适当的字符编码处理

## 7. 测试策略

### 功能测试
- 三种格式的导出功能测试
- 不同数据类型的兼容性测试
- 空结果集和错误处理测试

### 性能测试
- 大数据集导出性能测试
- 并发导出压力测试
- 内存使用情况监控

### 兼容性测试
- 不同浏览器下载功能测试
- Excel文件版本兼容性测试
- 中文和特殊字符处理测试

## 8. 总结

导出功能设计遵循了以下原则：
1. **用户友好**: 简洁的交互流程和清晰的反馈
2. **技术合理**: 合适的技术选型和架构设计
3. **性能优化**: 内存管理和数据限制策略
4. **扩展性强**: 易于添加新格式和功能
5. **安全可靠**: 完善的错误处理和资源保护

该功能为数据库查询工具增加了重要的数据输出能力，使用户能够方便地将查询结果用于各种业务场景。